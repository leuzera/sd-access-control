syntax = "proto3";

package person_auth;

service PersonAuth {
    rpc authPerson(Person) returns (Auth) {}
}

message Person {
    string type = 1
    string token = 2
}

message Auth {
    bool authorized = 1
}